$schema: "https://json-schema.org/draft/2020-12/schema"
description: an entry in the aligned bam/cram manifest
properties:
  sampleid:
    type: string
    description: "external ID for sample"
  alignment:
    type: string
    description: "absolute or relative path to post-BQSR bam or cram file for sample"
required:
  - sampleid
  - alignment
additionalProperties: no
