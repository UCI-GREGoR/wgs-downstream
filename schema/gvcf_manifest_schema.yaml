$schema: "https://json-schema.org/draft/2020-12/schema"
description: an entry in the g.vcf manifest
properties:
  sampleid:
    type: string
    description: "external ID for sample"
  vcf:
    type: string
    description: "absolute or relative path to .vcf.gz file for sample"
  gvcf:
    type: string
    description: "absolute or relative path to g.vcf.gz file for sample"
required:
  - sampleid
  - gvcf
additionalProperties: no
